# 每周综合复盘 · 2026-W38

> 复盘周期：**2026-09-16 22:45 → 2026-09-18 18:00**（承接 09-16 停机补跑；09-05～09-16 窗口见文末**附录 A**）
> 生成时间：2026-09-18 18:10 · 自动 cron（每周五 18:00）
> 本文件分两段：**主报告**＝本次 W38 例行复盘；**附录 A**＝09-16 停机后补跑报告（原文保留，标题降级）

---

## 零、本周核心发现（≤3）

1. 🔴 **新增系统性缺陷：state.db 间歇性 I/O 故障** —— 09-17～09-18 共 4 次 `disk I/O error`，命中 `PRAGMA journal_mode=WAL`，同时造成三类后果：`delegate_task` 派发失败 ×2（09-17 12:24）、`session_search` 工具不可用 ×2（09-18 09:06 / 10:01）、**09-17 09:17 之后多条 cron 会话静默不落库**（run 显示 completed，但 state.db 里查不到）。已排除磁盘满（185G 空闲）与库损坏（`quick_check` = ok，923MB / 6.35 万条消息）。
2. ✅/⚠️ **三处存量隐患处理**：my-coffee MCP **配置侧已停用**（`enabled: false`）—— 它的 401 重试占了 `gateway.error.log` 的 **93%／27.5 万行**，但**运行中的 gateway 仍走缓存配置，实测 18:05 又刷了一条，需重启才真正生效**；另两项无需你动手（`@larksuite/cli` 1.0.95 → **1.0.96**、**user token 刷新**（access token 今天 16:42 已过期，不处理则用户态飞书调用全部失效；refresh 延至 09-25 18:02）。
3. 🟡 **非流式 stall 第三次周期复发，但危害降级**：09-17 09:16 认知对话再次 600s 无响应，这次**被重试层自动兜住**、10 分钟后成功（W38 那次是直接失败）。累计 8 次（W36 3 + 09-07 4 + 09-17 1）。另一次性 bot 入会脚本因 UTF-8 解码失败报错，**当天 21:04 已修**。

---

## 一、协作质量审计

### 会话全景（窗口内 09-16 22:45 → 09-18 18:00）

| 时间 | 来源 | 标题 | 规模 | 性质 |
|------|------|------|------|------|
| 09-16 22:52 | feishu | 在么 | 7 msgs | 连通性确认（用户说明停机原因见下） |
| 09-17 09:04 | feishu | 旁听会议 静默即可 | **225 msgs** | 🟡 高消耗多任务会话 |
| 09-17 09:05 | cron | 每日认知对话 | 38 msgs | 🔴 含 stall 事件，重试成功 |
| 09-18 09:05 | cron | 每日认知对话 | （未落库） | ✅ 正常，122s |
| 09-18 14:41 | feishu | 旁听会议 as user | 63 msgs | 凯迪仕 AI 先锋大赛 → 3 张卡片 |

**重要背景更新**：W37 的 6.5 天停机根因本次由用户亲口说明 ——「我向公司申请了白名单，所以才能继续使用」，即**公司网络管控**导致，非机器故障。用户已取得白名单并恢复。这解释了 W37 缺口，也意味着**同类外部管控风险仍可能再现**。

09-17 那场会话一口气完成了 7 件事：3 场会议旁听（南区豆包周周碰 / 飞书 AI 绝活大会 LIVE #9 / 拓竹×限制切换租户）、按身份筛活动案例 highlight、2 篇文章拆解、妙记授权（domain minutes 需用户手动同意）、19:00 会议 bot 入会 + 跟进。**单会话多任务并行是本周最高消耗形态**。

### 本周返工统计

| # | 时间 | 事件 | 根因分类 | 是否已结构性修复 |
|---|------|------|---------|-----------------|
| 1 | 09-17 | 「我看完还是**没看懂**，到底他发生什么事了？」——文章拆解首版直接上了抽象骨架 | **输出结构缺失**：未按「先讲发生了什么 → 再上骨架」 | ✅ **已修**：`long-transcript-digest` skill 已写入这条反馈（含你的原话作为反面案例），并把「第一块必须是『发生了什么』」定为主规则 |
| 2 | 09-17 19:01 | 「**入会了吗**」——用户不确定 bot 是否已入会，主动追问 | **状态不可见 + 工具失败**：定时入会脚本 UTF-8 报错，且成功后无回执 | 🟡 **部分**：脚本 bug 当天已修（`env -i` 无 locale 复现法已沉淀进 skill）；「入会成功即刻回报一行回执」尚未固化成硬规则 |

**返工率**：3 个用户会话中 1 个明确返工 + 1 个确认摩擦 ≈ **33%**；两次均为**单轮即可恢复**，无「说了一遍还错」。

### 系统失败事件（窗口内 5 类）

| # | 时间 | 事件 | 根因 | 影响 |
|---|------|------|------|------|
| 1 | 09-17 09:16 | 认知对话 `Non-streaming API call timed out after 600s` | 模型/平台（老问题） | 🟡 重试后 09:17:59 成功，延迟 +10 分钟，**零内容损失** |
| 2 | 09-17 12:24 ×2 | `delegate_task` 派发失败，traceback 落在 `apply_wal_with_fallback` | 🔴 **state.db I/O** | subagent 未派出，会话改走终端路径完成 |
| 3 | 09-17 18:59 | 一次性脚本 job `31438dde206a`（19:00 bot 入会）script failed | 脚本 bug（bash 无 locale + `$VAR` 后紧跟中文） | 🟡 当天 21:04 修复；用户手动路径使 bot 正常入会 |
| 4 | 09-18 09:06 / 10:01 | `session_search` 返回 `Session database not available: disk I/O error` | 🔴 同 2 | 任务改用 sqlite / 终端直查完成，未中断 |
| 5 | 09-17 09:17 起 | **多条 cron 会话未落库**（09-17 21:00 摘要、09-18 09:05 复盘、09-18 10:00 同步） | 🔴 同 2（推断） | 会话历史缺失，细节需靠 `agent.log` 重建 |

### 模式问题（系统性缺陷，≥2 次）

| 缺陷 | 本期次数 | 累计 | 状态 |
|------|---------|------|------|
| 🔴 **state.db 间歇 I/O 故障** | 4 次（跨 2 个子系统 + 会话未落库） | 新增 | ⚠️ **已定位症状与判据，未根治**。DB 完好、空间充足 ⇒ 瞬时 I/O/锁，**不要在 gateway 运行时 VACUUM**；已按「记录时刻 → 受影响任务重试 → 持续则申请维护窗口」处理 |
| 🔴 **非流式 stall 超时** | 1 次 | W36 3 + W38 5 = **8 次** | ⚠️ 缓解到位（fallback 链 + `api_max_retries 5` 本次自动救回），但**根治方案 A/B/C 连续三个周期未落地** |
| 🟡 **cron 脚本 locale/变量名类 bug** | 1 次 | 2 次 | ✅ 已修 + 已沉淀 `env -i HOME=... bash script` 复现法 |
| 🟡 **技能体积 vs 非流式上限** | `ebbinghaus-review/SKILL.md` 仍 **32,218 B** | 3 个周期 | ⚠️ 未拆薄（方案 C 未执行） |
| 🟡 **日志体积无体检** | `gateway.error.log` 47.8MB / 29.5 万行，其中 my-coffee 占 **93%** | 新增 | 🟡 污染源已停用；日志文件本身未做轮转（gateway 持有句柄，运行中截断会产生稀疏文件，本期不动） |

### 本期结构性修复清单

| 修复 | 状态 |
|------|------|
| my-coffee MCP 停用（`enabled: false`，config.yaml 已备份） | ✅ 本次执行 |
| `@larksuite/cli` → 1.0.96（升级后 auth 复检 ready / valid） | ✅ 本次执行 |
| lark-cli user token 触发刷新（避免强制重新扫码） | ✅ 本次执行 |
| 「先讲发生了什么」写入 `long-transcript-digest` | ✅ 已在位（09-17/18 落地） |
| `hermes-cron-troubleshooting` 新增「state.db disk I/O 故障」章节 | ✅ 本次执行（复盘沉淀） |
| 入会成功回执规范（治「入会了吗」） | ❌ 未落地（见三·4） |
| stall 根治方案 A/B/C | ❌ 三周期未动 |
| 哨兵 watchdog job | ❌ 仍未安装（8 个 job 里无任何 no_agent 哨兵，文档与实况脱节） |

---

## 二、配置与系统审计

### 1. 核心 CLI 与凭证

| 项 | 状态 |
|----|------|
| lark-cli user token | 🔴→✅ 初始 `needs_refresh`（access **09-18 16:42 已过期**）→ 触发 API 自动刷新 → `valid`；**access 至 09-18 20:02，refresh 至 09-25 18:02** |
| lark-cli bot | ✅ ready（App `cli_a964fd626078dcbc`），扩展名/技能同步无告警 |
| @larksuite/cli 版本 | ✅ 1.0.95 → **1.0.96**（`changed 7 packages in 2s`），升级后 auth 复检 ready/valid |

### 2. npm 全局包

✅ **无 MCP 残留、无垃圾包**。13 个包全部合法：@larksuite/cli、@customer360/lark-c360、@bytedance-seed/kaboo-cli、@lark-project/meegle、@agent-tars/cli、@openai/codex、obsidian-cli、pinchtab、puppeteer-core、sharp、weread-agent-cli、claude-lifecycle-hook-logger、codex-lifecycle-hook-logger。

### 3. state.db / 存储层

- 体积 **923 MB**（63,500 条消息 / 766 个会话 / 9 个 FTS 索引）—— 单条消息均值 ≈14KB（工具输出占大头）
- `PRAGMA quick_check(1)` → **ok**（16s）；磁盘 `/System/Volumes/Data` **185 G 可用（58% 使用）**
- 间歇 `disk I/O error`（见一·3/4）已按「观察 + 重试」处置，**未做任何破坏性操作**

### 4. skills 目录

- **243 个 SKILL.md**（上次 242，+1）；无重复 description、无孤立空目录
- 🟡 **三胞胎仍在**（连续两周标记）：`note-taking/obsidian-plugin-maintenance`、`obsidian-plugin-troubleshooting`、`obsidian-vault-admin`，三者 description 近同义、各自 14 天内仅用 1 次 → **建议合并为 1 个（需你点头，curator 不做同义合并）**

### 5. cronjob 列表

8 个 job：**6 个常驻 active** + 2 个一次性（`31438dde206a` 入会脚本＝已完成并 disabled、`249729176856` 纪要＝已完成并 disabled），本期无「应启用却停用」的 job。

| Job | 计划 | 本期 | 状态 |
|-----|------|------|------|
| 每日认知对话 | 5 9 \* \* \* | 09-17（stall 后重试成功）/ 09-18 | ✅ |
| 每日工作摘要 | 0 21 \* \* \* | 09-17（silent，卡片已发，输出 13.5KB） | ✅ |
| 每周综合复盘 | 0 18 \* \* 5 | 本次 | ✅ |
| 每周深读推荐 | 0 10 \* \* 0 | 09-16 补跑后，下次 09-20 | ✅ |
| 双周Skill同步 | 0 10 \* \* 5 | 09-18 10:08 完成 | ✅ |
| AI赢单商机雷达 | 0 9 \* \* 1（双周） | 09-16 补跑后，下次 09-21 | ✅ |
| 一次性 ×2 | once | 09-17 20:05 完成 / 19:00 script failed（已修） | ⚪ 已停用 |

### 6. config.yaml 关键配置

| 项 | 值 | 判定 |
|----|----|------|
| 默认模型 | `deepseek-v4-flash` + routing 关闭 | ✅ 与「省钱优先」一致 |
| fallback_providers | `minimax-openai / MiniMax-M3` | ✅ 在位且本期实际生效（09-17 stall 兜底） |
| **mcp_servers.my-coffee** | **`enabled: false`（本次修改，已备份 config.yaml.bak-20260918）** | ✅ 死凭证（401）已停用；重启方式：`hermes mcp login my-coffee` 或改回 `true` |
| cron.wrap_response | true | ✅ |
| streaming.enabled | false | 🟡 stall 类故障的放大器（老问题） |
| timezone | **空** | 🟡 建议显式设 `Asia/Shanghai`（`hermes config set timezone Asia/Shanghai`，1 分钟） |
| approvals.cron_mode | `deny` | ✅ cron 内高危动作被拦（`execute_code` 已按预期拒绝） |

### 7. cron 成本（usage_audit，窗口内 11 次 run）

| Job | 次数 | input | output |
|-----|-----|-------|--------|
| 每日工作摘要 | 2 | **4.08 M** | 39.8 K |
| 每日认知对话 | 3 | 3.75 M | 51.7 K |
| 双周Skill同步 | 2 | 3.41 M | 35.3 K |
| 每周综合复盘 | 1 | 3.03 M | 30.2 K |
| 每周深读推荐 | 1 | 2.06 M | 16.1 K |
| AI赢单雷达 | 1 | 1.12 M | 21.4 K |
| 一次性纪要 | 1 | 0.42 M | 9.5 K |
| **合计** | **11** | **17.87 M** | **203.9 K** |

🟡 **单次最高**：每日工作摘要 09-17 = **3.86 M input**，是全库最贵的单次运行（同类任务重复读全量会话历史所致）→ 列为待优化项。

### 8. Obsidian 仓库

- 本地与远端 **0 ahead / 0 behind**（最后 commit `ae76c78` 每周深读 09-16）
- 工作区 **33 个未提交改动**（.obsidian 插件状态 + concepts 主题 + AI场景 新文件）——非本次引入，未擅动
- **reviews/ 仅 `2026-W28.md`、`2026-W30.md` 入库** ✅ 既定策略保持（本报告不 push）
- 🔴 `obsidian-project-hub` visibility 仍为 **PUBLIC**（9-16 有推送），连续两周待你拍板

---

## 三、技能库审计

### 1. 总量与更新

- **243 个 SKILL.md**（上次 242）；窗口内实际更新 **7 个**：`lark-cli`、`sales-playbook`（09-17）、`c360-cli`、`long-transcript-digest`（09-18）、`hermes-cron-troubleshooting`（09-17 + 本次）、`biweekly-skill-sync`（09-18 同步任务自更新）、`ai-win-opportunity-radar`（09-16）

### 2. 使用情况（use_count）

| Skill | 次数 | Skill | 次数 |
|-------|------|-------|------|
| feishu-meeting-listen | 366 | feishu-card-send | 145 |
| feishu-api（**已废弃**） | 128 | ebbinghaus-review | 115 |
| daily-session-summary | 95 | obsidian | 82 |
| meeting-audit | 81 | agent-group-collab | 75 |
| feishu-group-chat | 67 | lark-cli | 54 |
| c360-cli | 43 | knowledge-capture | 39 |

### 3. 30 天未使用候选（candidate for cleanup）

- **有使用记录但 30 天+未动**：`feishu-api`（08-04，128 次，**已废弃 → 建议归档**）、`my-coffee`（07-16，与本次停用同源）、`meeting-audit`（08-27）、`agent-group-collab`（08-18）、`feishu-group-chat`（08-17）、`knowledge-capture`（08-23）、`lark-doc`（08-17）、`beautiful-feishu-whiteboard`（07-29）、`multi-agent-orchestration`（07-22，stale）、`hermes-feishu-gateway`（07-25，stale）、`miaoda-deploy`（07-21，stale）
- **从未使用（use_count=0）**：87 个，绝大多数是标准库（arxiv / notion / powerpoint / openhue / polymarket…）

### 4. 缺失技能覆盖（按返工/故障根因反推）

| 根因 | 覆盖状态 |
|------|---------|
| state.db I/O 故障 | ✅ **本次已补**（`hermes-cron-troubleshooting` 新增章节：症状三态 / 排查命令 / 为什么不要 VACUUM） |
| 入会状态不可见（「入会了吗」） | ❌ **无覆盖**——建议在 `feishu-meeting-listen` 加硬规则：join 成功后**必须立刻回报一行回执**（会议号 + 主题 + 身份），用户不必追问 |
| 日志体积无体检 | ❌ **无覆盖**——无任何机制在 `gateway.error.log` 涨到 47MB 时报警；建议复盘任务加一条体积检查（本期已人工记录） |
| Obsidian 插件类三胞胎 | ❌ **无覆盖**——curator 只归档不合并，需人工决定 |
| 非流式 stall | 🟡 部分覆盖（fallback 已生效；哨兵未装、方案 C 未做） |

---

## 四、紧急待办处理情况

| 待办 | 处理 |
|------|------|
| 🔴 **lark-cli user access token 09-18 16:42 已过期** | ✅ **已解决** —— 触发 API 自动刷新；**access 有效期至 2026-09-18 20:02，refresh 至 2026-09-25 18:02**（原 09-25 14:42）。避免了一次强制重新扫码授权 |
| **CLI 版本过旧** | ✅ **已升级** —— `npm install -g @larksuite/cli@latest`，1.0.95 → **1.0.96**；升级后 auth 复检 user `valid` / bot `ready` |
| **MCP 死配置刷屏（my-coffee）** | 🟡 **配置侧已停用，待重启生效** —— config.yaml 加 `enabled: false`（改前已备份 `config.yaml.bak-20260918*`），`hermes mcp list` 显示 `✗ disabled`；但 gateway 进程持有缓存配置，**18:05:06 仍刷了一条**，重启后才真正止住。将来要恢复：`hermes mcp login my-coffee` |
| **npm 垃圾包 / MCP 残留** | ✅ **检查通过** —— 13 个全局包全合法，无需 uninstall |
| **需你手动操作 ①：重启 gateway（让 my-coffee 停用生效）** | ⚠️ **需你操作**：终端执行 `hermes gateway restart`，**预计 1 分钟** —— 请在**本次复盘跑完之后**再执行（重启会终止正在运行的 agent 进程，含今天的 cron 任务）。不重启只是每 5 分钟多一条 401 WARNING，不影响功能 |
| **其余** | ✅ 能在自动侧处理的都已处理 |

### ⚠️ 需你决策（均非阻塞，连续周期未决项已标注）

| # | 事项 | 建议动作 | 预计耗时 |
|---|------|---------|---------|
| 1 | **stall 根治方案 C**（`ebbinghaus-review/SKILL.md` 32KB 拆到核心 8KB，不动代码/模型） | 三周期未动；让我下次直接执行 | 15 分钟 |
| 2 | **每日工作摘要单次 3.86M token** 优化（先按会话聚合去重再进上下文） | 列为成本优化项，下期评估 | 20 分钟 |
| 3 | **obsidian 三胞胎合并**（maintenance / troubleshooting / vault-admin → 1 个） | 我合并，你点头即可 | 10 分钟 |
| 4 | **`obsidian-project-hub` 可见性**（仍 PUBLIC，concepts 已在其中） | `gh repo edit kitsiu1211-stack/obsidian-project-hub --visibility private`；**未决前 reviews 继续不入库** | 1 分钟 |
| 5 | **config timezone 为空** | `hermes config set timezone Asia/Shanghai` | 1 分钟 |

---

## 五、下一周期关注点

1. **my-coffee 停用待重启生效**：配置侧已完成（`✗ disabled`），但运行中的 gateway 仍按缓存配置重试（18:05:06 实测再现 1 条）。**需你执行一次 `hermes gateway restart`（1 分钟）**；不重启也只是每 5 分钟一条日志，无功能影响。
2. **state.db I/O 故障是否再现**：记录「时刻 + 命中子系统 + 是否导致会话未落库」；若再现 ≥2 次，申请**停 gateway 的维护窗口**做 `VACUUM INTO` 重建（不在运行中 VACUUM）。
3. **09-20 10:00 每周深读** / **09-21 09:00 AI赢单雷达（双周）** 是否按期触发（补跑后首次回归正常节奏）。
4. **09-25 18:00 下次复盘（W39）**：本期未决的 5 项会再点一次，若仍未拍板则按「最小改动优先」自行推进方案 1（拆薄 skill）。
5. **入会回执硬规则**：下次 bot 入会任务若无回执导致你再追问「入会了吗」，即视为返工，我会直接把规则写进 `feishu-meeting-listen`。

---

*生成：2026-09-18 18:10 · Hermes 每周综合复盘 cron（38603666df9f）· 主报告 + 附录 A（09-16 停机补跑原文）*

---

## 附录 A｜2026-09-16 停机补跑报告（覆盖 09-05～09-16，原文保留）

> 复盘周期：**2026-09-05 ～ 2026-09-16**（跨 W37 + W38）
> 生成时间：2026-09-16 22:45 · 自动 cron（每周五 18:00，本次为停机后补跑）
> ⚠️ **归档缺口说明**：`2026-W37.md` 未生成——机器 09-10 09:32 起停机，09-11 18:00 的 W37 例行复盘错过且未被补跑。本报告覆盖 W37+W38 两个窗口，不单独补 W37 文件。

---

### 零、本周核心发现（≤3）

1. 🔴 **机器停机 6.5 天（09-10 09:32 → 09-16 22:38）导致 17 次 cron 计划执行全部错过**，且 09-16 22:38 的补跑机制**每个 job 只回放 1 次**（`catch_up_occurrences=14`），6 天的每日认知对话/工作摘要**没有逐日补跑**，内容永久缺失。
2. 🔴 **W36 标记的「模型 stall 超时防护」缺口本轮复发并升级**：09-07 单日 4 次 cron 失败（认知对话 ×3 + 工作摘要 ×1），根因已彻底定位到代码层（cron 平台硬编码走非流式 → DeepSeek 非流式 stall），但当时候选的 3 个修复方案**一个都没落地**——ebbinghaus-review skill 至今仍是 32KB / cron job 仍跑 deepseek-v4-flash / 代码 589 行 `platform=="cron"` 判据仍在。唯一生效的是 fallback 链（MiniMax-M3）。
3. ✅ **救回一次强制重登录事故**：lark-cli user **refresh token 原定 09-17 12:26 到期**（不足 14 小时），即当天不处理、次日 12:26 后所有用户态飞书调用将失效、需人工重新扫码授权。已当场触发 API 刷新，refresh 有效期延至 **09-23 22:38**。另升级 @larksuite/cli 1.0.93 → 1.0.95 并同步其内置 skills。

---

### 一、协作质量审计

#### 本周会话全景

| 口径 | 数量 |
|------|------|
| 全部会话（09-05 起） | 26 个（cron 20 + feishu **5** + subagent 1） |
| **W38 窗口（09-14 起）** | 6 个，**全部是 cron**——零用户会话 |
| 用户实质 feishu 会话 | 5 个 |

**关键事实：09-11 → 09-16 完全零用户交互**（机器停机），所以 W38 本身**没有可审计的协作过程**。本节的返工统计因此以 09-05 ～ 09-10 的用户会话为对象。

用户 feishu 会话明细：

| 时间 | 标题 | 规模 | 性质 |
|------|------|------|------|
| 09-05 11:05 | GPT-6 能做的操作，像豆包工作桌面端… | 29 msgs / 12 tools | 正常探讨 |
| 09-07 09:46 | **每日认知对话又出错了** | **115 msgs / 56 tools** | 🔴 **返工会话**（cron 故障排查） |
| 09-08 17:30 | 你还在么 | 70 msgs / 24 tools | 正常（含交接上下文） |
| 09-08 17:30 | [Replying to: 公众号文章链接] | 31 msgs / 14 tools | 正常（文章分析） |
| 09-10 09:32 | **加入会议 as bot** | **132 msgs / 62 tools** | 🟡 高消耗任务（会议 bot 入会） |

#### 本周返工统计

**用户主动纠正/重新指示：2 次**

| # | 时间 | 事件 | 根因分类 | 是否已结构性修复 |
|---|------|------|---------|-----------------|
| 1 | 09-07 09:46 | 「每日认知对话**又**出错了」——同日该 job 已连挂 3 次，用户直接来报障 | **工具失败（模型/平台层）**：cron 平台被硬编码走非流式 API（`chat_completion_helpers.py:589` `platform=="cron" → direct_api_call`，为绕 worker 死锁 #62151），撞上 DeepSeek 非流式 stall → idle 600s watchdog 杀进程 | 🟡 **部分**——fallback 链（MiniMax-M3）已生效并于 09-08/09-10 成功兜住；但 3 个候选根治方案（A 加 cron_streaming 开关 / B cron 切非 DeepSeek / C 把 32KB skill 拆薄）**均未执行** |
| 2 | 09-08 17:40 | 「然后看看**刚才我说的那个事情**」——用户需重述上下文才能继续（齐俊元 Today AI / 敢忘 话题） | **流程问题（上下文丢失）**：长会话后话题漂移，无结构化台账承接 | ❌ 未修复（但当次由用户侧补一句即恢复，代价极低） |

**返工率**：5 个用户会话中 2 个含返工信号 ≈ 40%；但均为**单轮即可恢复**的轻度返工，无「说了一遍还错」的重复纠正。

> 注：关键词扫描曾报 25/64 条用户消息含纠正词，**逐条核对后全部为 skill 调用样板文本的误命中**（cron job 的 `[IMPORTANT: The user has invoked...]` 包装），已剔除，未计入。

#### 系统失败事件（本轮 5 次）

| # | 日期 | 事件 | 根因 | 影响 |
|---|------|------|------|------|
| 1 | 09-07 09:23 | 认知对话 `RuntimeError: Response remained truncated after 4 continuation attempts` | 非流式 stall（同上） | 当日复习中断 |
| 2 | 09-07 10:21 | 认知对话 TimeoutError（idle 939s） | 同上 | 同上 |
| 3 | 09-07 13:21 | 认知对话 TimeoutError（idle 940s） | 同上 | 同上（当日第 3 次） |
| 4 | 09-07 21:12 | 工作摘要 TimeoutError（idle 970s） | 同上 | 内容零损失 |
| 5 | 09-10 → 09-16 | **17 次计划执行错过**（见下表） | **环境（机器关机）** | 🔴 每日类任务累计 13 次缺跑，**未逐日补跑** |

#### 模式问题（系统性缺陷，≥2 次）

| 缺陷 | 本轮次数 | 累计 | 状态 |
|------|---------|------|------|
| 🔴 **非流式 stall 型超时** | 4 次（09-07 单日） | W36 3 次 + 本轮 4 次 = **7 次** | ⚠️ **确认未修复**。根因已定位到具体代码行，但候选修复全部未落地 |
| 🔴 **停机期间的每日任务缺口不补跑** | 17 次错过 → 仅 1 轮补跑/job | W35 起累计 ≥2 次 | ⚠️ **新增系统性缺陷**。补跑机制按 occurrence 折叠，每日任务缺跑 6 天 = 永久缺失 6 天内容 |
| 🟡 **技能体积 vs 非流式响应上限冲突** | 1 次（09-07 截断） | 2 次 | ⚠️ ebbinghaus-review SKILL.md = **32,218 字节**，正是 09-07 截断的直接诱因，未拆薄 |

#### 本轮结构性修复清单

| 修复 | 状态 |
|------|------|
| fallback provider 链（DeepSeek → MiniMax-M3） | ✅ 已在位且**实际生效**（09-07 故障排查会话本身跑在 MiniMax-M3 上；09-08/09-10 job 恢复正常） |
| `hermes-cron-troubleshooting` skill 沉淀 | ✅ 09-07 16:50 建立/更新，故障分类与修复路径已文档化 |
| cron_streaming 开关（方案 A） | ❌ 未执行 |
| cron job 切非 DeepSeek 模型（方案 B） | ❌ 未执行（仍为 `deepseek-v4-flash`） |
| ebbinghaus-review skill 拆薄（方案 C） | ❌ 未执行（仍 32KB） |
| Layer-3 哨兵 watchdog | ❌ **未安装**——skill 文档写明「dual sentinels at 9:35/10:05 now catch and rerun it」，但 `jobs.json` 里 **6 个 job 全部 `no_agent=False`、`script=None`，无任何哨兵 job**。文档化的修复从未落库 |

---

### 二、配置与系统审计

#### 1. 核心 CLI 状态

| 项 | 状态 |
|----|------|
| lark-cli user token | ✅ 初始 `needs_refresh`（refresh 原定 **09-17 12:26 到期，仅剩 14h**）→ 触发 API 自动刷新 → `valid`，refresh 延至 **2026-09-23 22:38** |
| lark-cli bot | ✅ ready（App `cli_a964fd626078dcbc`） |
| @larksuite/cli 版本 | ⚠️→✅ 1.0.93 → **1.0.95**（本次已升级，changed 7 packages） |

#### 2. npm 全局包

✅ **无 MCP 残留、无垃圾包**。13 个全局包全部合法：@larksuite/cli、@customer360/lark-c360、@bytedance-seed/kaboo-cli、@lark-project/meegle、@agent-tars/cli、@openai/codex、obsidian-cli、pinchtab、puppeteer-core、sharp、weread-agent-cli、claude-lifecycle-hook-logger、codex-lifecycle-hook-logger。**未见** `@m1heng-clawd/feishu`、`openclaw`、`openclaw-team-in-feishu`、`@overlink/openclaw-feishu`、`clawhub` 等历史残留。

#### 3. skills 目录整洁度

- **242 个 SKILL.md**（W36 = 241，+1）
- 无重复 description、无孤立目录
- 🟡 **发现 3 个高度重叠的 skill**（均在 30 天未使用区间）：
  - `note-taking/obsidian-plugin-maintenance`
  - `note-taking/obsidian-plugin-troubleshooting`
  - `note-taking/obsidian-vault-admin`
  三者 description 几乎同义（"Obsidian plugin won't load / needs updating"）。**建议合并为 1 个**——curator 只管归档不管合并，需人工决定。

#### 4. cronjob 列表

6 个 active job 全在列、全部 enabled；**但本轮全部为补跑**（09-16 22:38 同 tick 齐发）：

| Job | 计划 | 本轮状态 | 停机期间错过 |
|-----|------|---------|-------------|
| 每日认知对话 | 5 9 * * * | ✅ 补跑中 | **6 次** |
| 每日工作摘要 | 0 21 * * * | ✅ 补跑中 | **7 次** |
| 每周综合复盘 | 0 18 * * 5 | 🔄 本次（W37 那次未补） | 1 次（W37） |
| 每周深读推荐 | 0 10 * * 0 | ✅ 补跑中 | 1 次（09-13） |
| 双周Skill同步 | 0 10 * * 5 | ✅ 已 completed（W38 偶数周，正常执行） | 0（W37 为奇数周，本就静默） |
| AI赢单商机雷达 | 0 9 * * 1（双周） | ✅ 补跑中 | 1 次（09-14） |
| **合计** | | | **17 次计划执行错过** |

`catch_up_occurrences = 14` — 补跑按 occurrence 折叠，**每 job 只回放 1 轮**，未做逐日补跑。

#### 5. config.yaml 关键配置

基本与既定决策一致，**发现 1 处漂移 + 1 处死配置**：

| 项 | 值 | 判定 |
|----|----|------|
| 默认模型 | `deepseek-v4-flash` + routing **关闭** | ✅ 与「省钱优先」决策一致 |
| fallback_providers | MiniMax-M3（`minimax-openai`，key 在 .env） | ✅ 在位 |
| streaming.enabled | **false** | 🟡 这是 stall 类故障的放大器（非流式 + 无 watchdog = 干等） |
| cron | `wrap_response: true` | ✅ |
| curator | enabled，stale 30d / archive 90d / interval 168h / backup keep 5 | ✅ 运行正常（last run 09-08） |
| timezone | **空** | 🟡 依赖系统本地时区，跨时区/夏令时下 cron 时间可能漂移，建议显式设 `Asia/Shanghai` |
| mcp_servers | ⚠️ 仅 `my-coffee`，**但每次启动 401 未授权** | ⚠️ **死配置**：`gwmcp.lkcoffee.com` 返回 `401 Unauthorized`，本次启动/每个 session 各重试一次，日志刷屏 8 条/轮 |
| **`~/.hermes/.mcp.json`** | ⚠️ 残留 **Feishu MCP**（`npx -y @larksuiteoapi/lark-mcp`）+ 明文 app secret | ✅ **本次已清理**（见「四」） |

#### 6. Obsidian 仓库状态

- 本地 **0 ahead**（最后 commit：09-10 认知对话；停机后无提交，符合预期）
- 工作区 **31 个未提交改动**（.obsidian 插件状态 / concepts 主题11、2、26、28、29、3、33、5、8、9 / AI场景新增文件）——非本次引入，未擅动
- `obsidian-project-hub` **visibility 仍为 PUBLIC**（最后更新 09-10）——W36 提出的可见性决策**用户尚未拍板**，因此本次延续既定策略：**复盘仅本地保存 + 同步 My Great Vault，不 push**
- reviews/ 入库情况：仅 `2026-W28.md`、`2026-W30.md` 在 git 中，W31+ 全部 untracked ✅ 策略保持

---

### 三、技能库审计

#### 1. 总量与更新

- **242 个 SKILL.md**（W36 = 241）
- **本轮实际更新 3 个**：`devops/biweekly-skill-sync`（09-04）、`research/wechat-article-extraction`（09-05）、`devops/hermes-cron-troubleshooting`（09-07）、`productivity/memory-management` + `productivity/dare-to-forget`（09-08）、`feishu/feishu-meeting-listen`（09-10）
- 新增 1 个：`dare-to-forget`（09-08，用户「敢忘」讨论落地）

#### 2. 使用情况

**近 14 天活跃 skill（use_count）**

| Skill | 使用 | Skill | 使用 |
|-------|------|-------|------|
| feishu-meeting-listen | 363 | ebbinghaus-review | 113 |
| feishu-card-send | 140 | daily-session-summary | 94 |
| obsidian | 81 | lark-cli | 51 |
| 文章分析 | 36 | weekly-deep-read | 21 |
| wechat-article-extraction | 21 | weread | 20 |
| ljg-rank | 21 | ljg-learn | 18 |
| skill-hub | 18 | 双周Skill同步 | 18 |
| hermes-agent | 16 | human-writing | 15 |
| ljg-plain | 13 | hermes-cron-troubleshooting | 8 |
| memory-management | 6 | ai-win-opportunity-radar | 6 |

#### 3. 30 天未使用候选（candidate for cleanup）

- **30–40 天区间（14 个）**：`lark-doc`(33)、`meeting-followup-material`(14)、`lark-minutes`(4)、`lark-vc`(1)、`ljg-card`(3)、`obsidian-vault-admin`/`obsidian-plugin-troubleshooting`/`obsidian-plugin-maintenance`(各 1)、`wayfinder-requirements`、`skill-routing`、`explain-like-village-elder`、`tech-project-evaluation`、`feishu-api`(128 次，已废弃)
- **40 天+（约 70 个）**：以标准库批量导入为主，由 curator 托管自动归档
- **从未使用（use_count=0，93 个）**：多为标准库（arxiv / notion / powerpoint / openhue / polymarket 等）；**但其中 3 个值得注意**：
  - `renewal-proposal`（state=stale，业务 skill，从未用过）
  - `yuanbao`（业务 skill，从未用过）
  - 🔴 `dare-to-forget`（09-08 刚建，**尚未被使用过**——用户当天讨论「敢忘」后建立，之后机器停机，机制尚未验证）
- 用户自有 skill（curator 不可改）均无过期。

#### 4. 缺失技能覆盖（按返工根因反推）

| 根因 | 覆盖状态 |
|------|---------|
| 非流式 stall 超时 | 🟡 **部分覆盖**——`hermes-cron-troubleshooting` 已文档化「fallback 链 + 哨兵 watchdog」双层方案，但**哨兵未安装**，文档与实况脱节 |
| 停机后的每日任务缺口 | ❌ **无覆盖**——无任何 skill/机制负责「检测停机窗口 → 逐日补跑每日类任务」。补跑由 gateway 内置逻辑折叠处理，不可配置 |
| 技能体积超非流式上限 | ❌ **无覆盖**——无「skill 体积体检」机制；32KB 的 ebbinghaus-review 无人报警 |
| Obsidian 插件类 skill 三胞胎 | ❌ **无覆盖**——需人工合并，curator 不做同义合并 |

---

### 四、紧急待办处理情况

| 待办 | 处理 |
|------|------|
| 🔴 **lark-cli user refresh token 14h 内到期** | ✅ **已解决**——`lark-cli im +chat-list` 触发自动刷新；token `valid`，**新 refresh 有效期至 2026-09-23 22:38**（原 09-17 12:26）。**避免了一次强制重新扫码授权** |
| **CLI 版本过旧** | ✅ **已升级**——`npm install -g @larksuite/cli@latest`，1.0.93 → **1.0.95**（changed 7 packages in 3m），升级后 auth 复检仍 `valid` / bot `ready` |
| **CLI 内置 skills 与二进制不同步** | ✅ **已解决**——发送卡片时 CLI 报 `skills 1.0.82 out of sync with binary 1.0.95`，已执行 `lark-cli update` 同步；复检 `_notice` 已消失 |
| **MCP 残留（文件层）** | ✅ **已清理**——`~/.hermes/.mcp.json` 中残留已废弃的 Feishu MCP（`npx -y @larksuiteoapi/lark-mcp`，含明文 app secret）。已备份为 `.mcp.json.bak-20260916223955`，内容重置为 `{"mcpServers": {}}`。符合「MCP removed，CLI-only」架构 |
| **npm 垃圾包** | ✅ **检查通过**——13 个全局包全合法，无需 uninstall |
| **配置漂移（旧 MCP 回调）** | ✅ **已清理**（同上） |
| **需你操作 ①：my-coffee MCP 凭证失效** | ⚠️ **需你操作**：每次 gateway 启动 + 每个 session 都 401 报错刷屏。修复路径：终端执行 `hermes mcp login my-coffee` 重新授权，**预计 2 分钟**。若已不用瑞幸点单，也可直接删 config.yaml 的 `mcp_servers.my-coffee` 块（预计 1 分钟） |
| **需你操作 ②：停机期每日任务缺口** | ⚠️ **需你决策**：当前补跑机制**每 job 只回放 1 轮**，停机 6 天 = 每日认知对话/工作摘要各永久缺 6 天。选项 A：接受现状（内容损失可接受，不折腾）；选项 B：给两个每日 job 加「按 output 目录逐日补跑」的岗哨脚本（预计 20 分钟，但 6 天积压一次性补跑会连发 12 张卡片）。**我倾向 A**——停机期间用户在休假，补跑历史复习无意义 |
| **需你决策 ③：stall 超时根治（沿用 W36 待决项）** | ⚠️ **需你决策**：已连续两个周期（W36 3 次 + W38 4 次 = 7 次）出现。三个候选修复一个都没落地。**最低成本的是方案 C**：把 `ebbinghaus-review/SKILL.md` 从 32KB 拆到核心 8KB——不动代码、不动模型配置，直接消除截断诱因。要我下次执行吗？（预计 15 分钟） |
| **需你决策 ④：GitHub 仓库可见性（沿用 W36 待决项）** | ⚠️ **需你决策**：`obsidian-project-hub` 仍为 **PUBLIC**，concepts 主题已在其中。选项 A：转 private——`gh repo edit kitsiu1211-stack/obsidian-project-hub --visibility private`，预计 1 分钟；选项 B：维持 public + reviews 永不入库（现状）。**决策前我不会 push reviews** |

---

### 五、下一周期关注点

1. **09-17 09:05 起观察**：认知对话/工作摘要恢复正常节奏后，是否再现 stall（fallback 已证明能兜住，但会静默降级到 MiniMax-M3，需注意答案质量与模型一致性）
2. **dare-to-forget skill 首次实战**：09-08 建立后从未运行，下次「敢忘/记忆审计」触发时验证其是否可用
3. **obsidian 三胞胎合并**：建议在 curator 下次运行（interval 168h）前人工合并为 1 个
4. **timezone 显式化**：config.yaml `timezone` 为空，建议设 `Asia/Shanghai` 以固定 cron 时刻语义

---

*生成：2026-09-16 22:45 · Hermes 每周综合复盘 cron（38603666df9f）· 本次为停机 6.5 天后的补跑*
