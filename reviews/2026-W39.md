# 每周综合复盘 · 2026-W39

> 复盘周期：**2026-09-19 00:00 → 2026-09-25 18:00**（上期 W38 覆 09-16 22:45 → 09-18 18:00）
> 生成时间：2026-09-25 18:15 · 自动 cron（每周五 18:00）
> 本周期特征：**用户输入低谷（四天 0 条消息）+ 存量系统缺陷由「间歇」转为「常驻」**

---

## 零、本周核心发现（≤3）

1. 🔴 **state.db 句柄污染由「间歇故障」升级为「常驻故障」，机制本次钉死**。09-25 18:00 复查时 `session_search` **当场报 `disk I/O error`**（不是历史记录）。新证据：gateway 进程（PID 1971）**同时持有 5 个指向旧 WAL inode 的 fd**（inode `94834555` / 8,796,232 B），而磁盘上的 `state.db-wal` inode 已换成 `95338643` → 几分钟后又变 `95740647`（0 字节）——WAL 被反复重建，进程句柄却钉在已删除的旧 inode 上。后果：`state.db` mtime **冻结在 09-24 21:09**、今天 0 条 message 落库、`session_search` 不可用（本报告 Part 1 只能走 sqlite3 CLI 直查）。`quick_check = ok`、磁盘 177G 空闲 ⇒ 文件没坏，坏的是**活进程的句柄**。
2. ⚠️ **同一个动作能同时结掉两笔存量隐患**：① 上面的句柄污染（修法＝重启，非 VACUUM）；② my-coffee MCP 401 噪音 —— 配置侧 09-18 已 `enabled: false`，但长跑进程仍按启动时缓存配置每 5 分钟重试一次，**今天单日 218 条 401**，把 `errors.log` 全天刷满。两者根因同源：**进程启动后配置不再重读**。
3. 🟡 **本周期是「安静的四天 + 两场用户会话」**：09-22～09-25 四天用户侧 0 条消息（除 cron），实质对话只有 09-20（148 msgs，两场会议旁听）与 09-21（25 msgs，`你研究一下 jev 模型`）。后者**会话在工具结果后中断、没有结论交付**，是本周唯一未闭环的用户任务；已由 09-21 建立的 `ai-model-due-diligence` skill 承接结构性修复。

---

## 一、协作质量审计

### 会话全景（窗口内 11 个 session / 190 条消息 / 87 次工具调用）

| 时间 | 来源 | 标题 | 规模 | 性质 |
|------|------|------|------|------|
| 09-20 15:03 | feishu | 旁听会议 as user #2 | **148 msgs** | 🟢 两场会议旁听（豆包工作伙伴分享）+ 追问「Claude Tag 是什么」「AGENTS.md 开放的意义」 |
| 09-21 14:12 | feishu | 你研究一下 jev 模型 | 25 msgs | 🔴 **会话中断，无 assistant 收尾**（末条为 14:13:49 工具结果） |
| 09-21 21:01–21:04 | cli ×4 | 每日工作摘要 Evaluator | 1–2 msgs/个 | 🟡 卡片质检，1 次 + 3 次重试 |
| 09-23 21:02 | cli ×1 | 评分每日工作摘要卡片 | 2 msgs | 🟡 当日无用户会话，卡片走质检 |
| 09-24 21:07–21:09 | cli ×5 | 评估每日工作摘要卡 | 1–4 msgs/个 | 🔴 **首版 FAIL → 修订 → 重评**，中途一次**输出长度截断** |
| 09-22 / 09-23 / 09-24 / 09-25 | — | — | 0 | 用户侧 0 条消息（四天） |

**结构性观察**：本周 11 个 session 里 9 个是 cron/质检会话，用户直接参与的只有 2 场。**cron 会话在 state.db 里本就不落库**（`source=cron`），所以「今天 0 个 session」不能直接判成故障 —— 这条判据本周期已写进 skill（见 Part 1 修复清单 #4）。

### 本周返工统计

| # | 时间 | 事件 | 根因分类 | 是否已结构性修复 |
|---|------|------|---------|-----------------|
| 1 | 09-21 | `你研究一下 jev 模型` —— 工具结果后会话中断，**用户问题未闭环、无结论交付** | **任务未闭环**（会话中断，非需求误解、非模型选错） | ✅ **已修**：`ai-model-due-diligence` skill（09-21 更新）把「新模型研究」标准化为「分离厂商口径 vs 实测边界」的固定产出 |
| 2 | 09-21 21:01 | 每日摘要 Evaluator 连开 **4 个 cli 会话**（1 正评 + 3 重试） | **cron 会话异常退出 + 卡片未一次过评** | 🟡 **部分**：skill 09-24 已写入「最多 3 轮」与 MiniMax `--reasoning low` 参数 |
| 3 | 09-24 21:07 | 摘要卡首版 **FAIL** → 修订 → 重评（5 个会话）；中途命中 **输出长度截断** | **单轮输出超长 + 事实材料过大**（4.4KB） | ✅ **已修**：skill 09-24 写入「事实材料压到 ~3.3KB、砍成短句列表」 |
| 4 | 09-23 / 09-24 | 连续两天把 state.db 状态**误判为「故障/停写」**（09-23 报 disk I/O、09-24 要求重启） | **判据缺失** —— 把「当天没有用户会话」直接当成「库停止写入」 | ✅ **已修**：skill 09-24 写入「权威三查」（Inbound 计数 / Lark 连接状态 / 飞书侧对账）；**本周期我又补上 inode 级定性法**（见修复清单 #1） |

**返工率**：用户侧实质会话 2 场、明确未闭环 1 场 ≈ **50%**（但**无「说了一遍还错」**）；cron 侧 3 天出现多轮返工，集中在每日工作摘要。

### 系统失败事件（窗口内 5 类）

| # | 时间 | 事件 | 根因 | 影响 |
|---|------|------|------|------|
| 1 | 09-25 18:00 | `session_search` 报 `disk I/O error` | **旧 WAL inode 句柄污染**（本次确诊） | 会话检索不可用；今天 0 条 message 落库 |
| 2 | 09-23 01:53–02:15 | Lark 长连接 **29 次重连失败**（`Failed to resolve 'open.feishu.cn'`） | 公司网络 DNS 解析失败（外部管控，同 W37 白名单问题） | 02:15 自动恢复，无消息丢失证据 |
| 3 | 09-25 全天 | my-coffee MCP 401 **×218**（每 5 分钟一次） | 长跑进程用缓存配置，`enabled:false` 未生效 | `errors.log` 被刷满，噪声淹没真错误 |
| 4 | 09-17 起累积 | cron 会话静默不落库（state.db mtime 冻结 09-24 21:09） | 同 #1 | 复盘/摘要类任务的历史可追溯性下降 |
| 5 | 09-20 10:02 | 深读推荐 output 命中关键词误报 | 文件里有 `## Error Handling` 章节标题 | 已排除，非故障（记录以免下次误判） |

### 模式问题（系统性缺陷，≥2 次）

1. 🔴 **P1｜gateway 长跑未重启（连续三周期）**：进程 PID 1971 自 **09-16 22:38** 起跑，已 **8 天 19 小时**。衍生三个症状：WAL 句柄污染（09-23/09-24/09-25）、缓存配置漂移（my-coffee 401）、CLI 升级后运行态未同步。W38 已列「需重启」，**至今未执行** → 本周期唯一硬性阻塞项。
2. 🔴 **P2｜每日工作摘要多轮返工（09-21 / 09-23 / 09-24）**：已通过 skill 两轮收窄（轮次上限、事实材料体积、权威判据），但底层仍受 #1 的不稳定影响。
3. 🟡 **P3｜静默期成本无闸门**：09-22～09-25 四天用户 0 输入，cron 仍全额运行 —— 本周 18 次 run 累计 **15.42M tokens**（其中 4 天静默期约占 9.5M）。

### 本期结构性修复清单

| # | 修复 | 载体 | 类型 |
|---|------|------|------|
| 1 | **旧 WAL inode 句柄污染的定性法**（`lsof -p <gw> \| grep state.db-wal` 看多 inode + `stat -f %i` 比对磁盘）+ 重启验证三连 + 「配置改了行为没变＝需重启」判据 + 长跑进程巡检 | `hermes-cron-troubleshooting`（本次 patch） | ① 技能沉淀 |
| 2 | 5 个**无 SKILL.md 的孤儿目录**（diagramming / domain / feeds / gifs / inference-sh，仅 2026-04-12 的 DESCRIPTION.md）移出 skills 树 | `~/.hermes/_skill_orphans_20260925/`（可还原） | ② 环境清理 |
| 3 | user token 当场刷新（09-24 23:01 已过期） | `lark-cli im +chat-list --as user` 触发 | ③ 凭证 |
| 4 | （承接 W38）每日摘要「权威三查」+ 事实材料上限 | `daily-session-summary`（09-24） | ① 技能沉淀 |
| 5 | （承接 W38）新模型研究标准化流程 | `ai-model-due-diligence`（09-21） | ① 技能沉淀 |

---

## 二、配置与系统审计

### 1. 核心 CLI 与凭证
| 项 | 状态 |
|----|------|
| `lark-cli` 版本 | **1.0.96**（最新，无需升级） |
| bot identity | ✅ ready |
| user access token | ⚠️→✅ **已过期（09-24 23:01）→ 当场触发刷新** → 新有效期 **2026-09-25 20:00** |
| user refresh token | ✅ **2026-10-02 18:00**（10-01 → 10-02，随刷新顺延） |
| 授权范围 | 完整（minutes / base / docs / im / vc / mail / task / okr 等全量） |

### 2. npm 全局包
✅ **无 Feishu MCP 残留、无第三方 feishu-cli 垃圾包**。13 个包全部合法：`@larksuite/cli`、`@customer360/lark-c360`、`@bytedance-seed/kaboo-cli`、`@openai/codex`、`obsidian-cli`、`weread-agent-cli`、`pinchtab`、`sharp`、`puppeteer-core`、`@agent-tars/cli`、`@lark-project/meegle` + 2 个 hook logger。**无需卸载。**

### 3. state.db / 存储层
| 指标 | 值 |
|------|-----|
| 体积 | 929.8 MB（63,706 条 message） |
| `quick_check` | ✅ ok（未损坏） |
| 磁盘 `state.db-wal` | 0 字节，mtime 09-24 21:10 |
| **进程持有的 `-wal` fd** | 🔴 **5 个指向旧 inode `94834555` / 8.4MB** + 1 个指向新 inode |
| 磁盘可用空间 | 177 GB（排除磁盘满） |
| 最后落库时间 | 🔴 2026-09-24 21:09（此后 0 条，含今天所有 cron 会话） |

### 4. skills 目录
- `~/.hermes/skills/` 顶层 88 项（原 93，清理 5 个孤儿目录后）、全树 **244 个 SKILL.md**
- 🔴→✅ 发现并清理 **5 个无 SKILL.md 的孤儿目录**（仅存 2026-04-12 的 `DESCRIPTION.md`）：`diagramming` / `domain` / `feeds` / `gifs` / `inference-sh` → 已移入 `~/.hermes/_skill_orphans_20260925/`（保留可还原）

### 5. cronjob 列表（8 个，全部 `last_status = ok`）
| 任务 | 节奏 | 上次运行 | 状态 |
|------|------|---------|------|
| 每日认知对话 | 每日 09:05 | 09-25 09:07 | ✅ ok |
| 每日工作摘要 | 每日 21:00 | 09-24 21:09 | ✅ ok（内容侧多轮返工见 Part 1） |
| 每周深读推荐 | 周日 10:00 | 09-20 10:02 | ✅ ok |
| 每周综合复盘 | 周五 18:00 | **本次** | ✅ running |
| 双周 Skill 同步 | 周五 10:00 | 09-25 10:00 | ✅ ok |
| AI赢单商机雷达 | 双周一 09:00 | 09-21 12:52 | ✅ ok |
| Anker 黑客松 · Bot 入会旁听 | 一次性 | 09-21 20:02 | ⏸ 已完成停用（正常） |
| Anker 黑客松 · 会后纪要 | 一次性 | 09-21 22:07 | ⏸ 已完成停用（正常） |

无失败、无「应启用却停用」的 job。

### 6. config.yaml 关键配置
| 项 | 值 | 评价 |
|----|----|------|
| `model` | deepseek-v4-flash（provider deepseek） | ✅ 符合「省钱优先」 |
| `fallback_providers` | MiniMax-M3 | ✅ 兜底在位 |
| `auxiliary.vision` | deepseek-v4-flash-vision-exp | ✅ |
| `tts.provider` | fish-audio | ✅ |
| `mcp_servers` | 仅 `my-coffee`（`enabled: false`） | 🟡 配置已停用，运行态未生效（需重启） |
| `curator` | enabled / interval 168h / stale_after 30d | ✅ |
| `approvals.cron_mode` | `deny` | ✅ 预期 —— cron 中 `execute_code` 被拦属安全策略，非缺陷 |
| `timezone` | **`''`（空）** | 🟡 W38 已提，连续未决 |

### 7. cron 成本（`usage_audit`，窗口内 18 次 run）
| 任务 | runs | input | output | total |
|------|------|-------|--------|-------|
| 每日认知对话 | 7 | 7,438,073 | 133,380 | **7,571,453** |
| 每日工作摘要 | 6 | 6,079,288 | 86,479 | **6,165,767** |
| 每周深读推荐 | 1 | 910,084 | 13,626 | 923,710 |
| Anker 会后纪要 | 1 | 649,541 | 12,060 | 661,601 |
| 双周 Skill 同步 | 1 | 55,135 | 329 | 55,464 |
| Anker 入会 | 1 | 20,878 | 782 | 21,660 |
| AI赢单雷达 | 1 | 19,276 | 154 | 19,430 |
| **合计** | **18** | **15,172,275** | **246,810** | **15,419,085** |

`error` 字段：**0 条**。⚠️ 其中约 9.5M 落在「四天无用户输入」的静默期。

### 8. Obsidian 仓库
- `~/Documents/Obsidian Vault` git 工作区干净，与远端 **0/0 同步**（最新提交：`64ec2ea 每日认知对话 2026-09-25`）
- `reviews/` 现有 W28 / W30 / W31 / W32 / W33 / W34 / W35 / W36 / W38 + **本次 W39**；**W29、W37 永久缺失**（停机 + 窗口缺口所致，不补跑）
- 按既定策略 `reviews/` 不入 public 仓库

---

## 三、技能库审计

### 1. 总量与更新
- 全树 **244 个 SKILL.md**；usage 索引追踪 **202 个**（active 103 / curator 标记 stale 99）
- **近 7 天更新 11 个**：`daily-session-summary`(09-24)、`ai-model-due-diligence`(09-21)、`long-transcript-digest`(09-20)、`sales-playbook`(09-20)、`feishu-meeting-listen`(09-20)、`ebbinghaus-review`(09-19)、`c360-cli`(09-18)、`hermes-cron-troubleshooting`(09-18 + **本次**)、`biweekly-skill-sync`(09-18)、`lark-cli`(09-17)、`ai-win-opportunity-radar`(09-16)

### 2. 使用情况（Top 10）
| skill | use_count | 最近使用 |
|-------|-----------|---------|
| feishu-meeting-listen | **368** | 09-21 |
| feishu-card-send | 157 | 09-25 |
| feishu-api | 128 | 08-04 |
| ebbinghaus-review | 122 | 09-25 |
| daily-session-summary | 105 | 09-25 |
| obsidian | 84 | 09-25 |
| meeting-audit | 81 | 08-27 |
| agent-group-collab | 75 | 08-18 |
| lark-cli | 55 | 09-25 |
| c360-cli | 43 | 09-18 |

### 3. 30 天未使用候选（candidate for cleanup）
- curator 自动标记 **99 个 stale**（09-23 那次 run：5 marked stale、1 reactivated；`llm: skipped (consolidation off)` ⇒ 只做时间维度、没做内容合并）
- `use_count = 0` 共 **86 个**，其中 13 个仍 active
- ⚠️ **计数有滞后，不能只凭 `use_count` 判死**：`article-analysis`、`biweekly-skill-sync` 显示 0 次但本周实际使用过 → 建议清理时以 **curator stale + mtime + 人工确认** 三者交叉，不做纯脚本删除
- 本周期未新增清理动作（上周期「obsidian 三胞胎」合并仍待决策，见 Part 4）

### 4. 缺失技能覆盖（按本周返工/故障根因反推）
| 缺口 | 反推依据 | 处置 |
|------|---------|------|
| **gateway 健康自检 / 进程卫生无单一入口** | WAL 句柄污染 + 缓存配置漂移 + 21 天长跑守护进程三件事分散，无一处汇总 | ✅ **本次已补**：`hermes-cron-troubleshooting` 增「旧 WAL inode 定性法」「同一个重启动作治两病」「长跑进程巡检」 |
| **静默期成本闸门** | 四天 0 用户输入仍 15.4M tokens | 🟡 待建：给重任务 cron 加「当天无用户会话则跳过/降级」前置判断 |
| **`reviews/` 周期缺口的索引标注** | W29 / W37 永久缺失，无索引说明 | 🟡 待办（低优先，1 次改动即可长期受益） |

---

## 四、紧急待办处理情况

### ✅ 已解决（本周期当场处理）
- ✅ **user token 已刷新**：原 access 已 **09-24 23:01 过期** → 我触发 `lark-cli im +chat-list --as user` → 新 access 有效期 **2026-09-25 20:00**、refresh **2026-10-02 18:00**
- ✅ **npm 无 MCP 残留**：13 个全局包全合法，**无需卸载**
- ✅ **CLI 已是最新**：`@larksuite/cli` 1.0.96
- ✅ **skills 孤儿目录已清理**：5 个（diagramming / domain / feeds / gifs / inference-sh）→ 移入 `~/.hermes/_skill_orphans_20260925/`，可随时还原
- ✅ **结构性沉淀**：`hermes-cron-troubleshooting` 新增 WAL inode 级定性法 + 重启验证三连（下次同类故障 1 条命令定性，不必再猜 3 个周期）

### ⚠️ 需你操作（唯一 1 件，一件事两个收益）
**重启 gateway** —— 预计 **1 分钟**，命令：

```
hermes gateway restart
```

- **在哪执行**：在 Mac 上**另开一个终端**（不要在 cron / 对话会话里跑）
- **什么时候**：**等本次复盘跑完再执行**（重启会终止正在运行的 agent 进程）
- **收益**：① 消除旧 WAL inode 句柄污染 → `session_search` 恢复可用、会话重新落库；② 让 my-coffee `enabled:false` 真正生效 → 每天少 288 条 401 噪音
- **验证三连**：
  1. `ls -la ~/.hermes/state.db` → mtime 随新会话往后推进
  2. `grep -c "my-coffee" ~/.hermes/logs/errors.log` → 不再增长
  3. `session_search` 能正常返回结果（不再报 `disk I/O error`）

### ⚠️ 需你决策（连续未决，均非阻塞 —— 保留不催）
- ① `timezone` 仍为空 → `hermes config set timezone Asia/Shanghai`（**1 分钟**）
- ② `obsidian` 三胞胎（`obsidian-plugin-maintenance` / `obsidian-plugin-troubleshooting` / `obsidian-vault-admin`）合并（**10 分钟**）
- ③ `obsidian-project-hub` 仍 public、`reviews/` 未入库（未决前维持现状）
- ④ 32KB 大 skill（`ebbinghaus-review`）拆薄
- ⑤ **新增观察**：会议自动检测守护 `poll-v5.sh`（PID 4051）自 **09-04** 起连续运行 **21 天**、10s 间隔轮询 —— 还在用就留着（会自动检测并入会），不用了告诉我停掉

---

## 五、下一周期关注点

1. **gateway 重启是否落地** —— 连续两周期未决。若 W40 仍未重启，我按「最小改动优先」自行评估是否升级处置（届时句柄污染已持续 4 周）。
2. **静默期成本闸门** —— 四天 0 输入仍花 15.4M tokens/周，建议给「认知对话 / 摘要 / 深读」三个重任务加前置判断。
3. **token 续期提醒** —— 本次 refresh 至 **2026-10-02 18:00**，10-02 前需再刷一次（否则用户态飞书调用全失效）。
4. **reviews 缺口标注** —— W29 / W37 永久缺失，建议一次性在 `reviews/` 加索引说明。
5. **09-25 review 归档** —— 按既定策略不入 public 仓库，仅本地 + My Great Vault 同步。

---

*数据来源：`~/.hermes/state.db`（sqlite3 直查）、`~/.hermes/cron/jobs.json` + `executions.db` + `usage_audit.jsonl`、`~/.hermes/logs/{gateway,agent,errors}.log`、`~/.hermes/skills/.usage.json`、`lsof -p 1971`、`lark-cli auth status`。*
*本报告由 Hermes 每周综合复盘 cron 自动生成。*
