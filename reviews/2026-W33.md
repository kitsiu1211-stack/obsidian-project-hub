# 每周综合复盘 · 2026-W33

> 复盘周期：2026-08-10（周一）～ 2026-08-14（周五）
> 生成时间：2026-08-14 18:05 · 自动 cron（每周五 18:00）

---

## 一、协作质量审计

### 本周返工统计

本周共识别 **3 次返工**，全部已在当周完成结构性修复：

| # | 日期 | 事件 | 根因分类 | 处理 |
|---|------|------|---------|------|
| 1 | 08-10 | 疆海会议旁听 poll.sh 字幕停止后进程空跑 4 小时不退出，纪要延迟 | 工具失败 | ✅ 08-11 修复 poll.sh，新增空字幕 N 轮主动退出 |
| 2 | 08-13 | 会议旁听用户退会后误判「会议结束」（--as user 报 120003） | 工具失败 | ✅ 改用 `listen_subtitles_bot.py`（bot 身份），status==ended 判结束，skill 已 patch |
| 3 | 08-14 | 用户发现主题 31 没写入 Obsidian，查出「双库分叉」 | 配置/状态漂移 | ✅ rsync 双向安全合并 + 记忆写入「双库 + 改完必同步」规则 |

**根因分布**：工具失败 ×2，配置/状态漂移 ×1。本周无「误解需求 / 模型选错 / 技能缺失」类返工。

### 模式问题（系统性缺陷）

| 缺陷 | 出现次数 | 状态 |
|------|---------|------|
| 会议旁听脚本「结束判断」误判 | 2 次（08-07、08-13） | ✅ 已彻底修复——从「用户身份轮询」重构为「bot 身份 + status==ended」双确认，根因已消除 |

### 本周结构性修复清单

1. **poll.sh**：空字幕 N 轮主动退出，不再靠 notify_on_complete 等资源释放（08-11）
2. **meeting-audit skill**：补入「用户退会误判」pitfall + bot 模式旁听步骤（08-13）
3. **Obsidian 双库规则**：记忆写入「Obsidian Vault（git 主库）+ My Great Vault（云端镜像），改完必须 rsync -a 同步」；obsidian / knowledge-capture 两个 skill 同步更新（08-14）
4. **wayfinder-requirements skill**：patch（08-10）

---

## 二、配置与系统审计

### 1. 核心 CLI 状态

| 项 | 状态 |
|----|------|
| lark-cli user token | ⚠️ 初始 needs_refresh → ✅ 已刷新，valid 至 08-14 20:03，refresh 至 08-21 18:03 |
| lark-cli bot | ✅ ready |
| @larksuite/cli 版本 | 1.0.84（记忆里的 1.0.77 已更新，无更旧残留） |

### 2. npm 全局包

✅ **无 MCP 残留、无垃圾包**。11 个全局包全部合法：@larksuite/cli、@customer360/lark-c360、@bytedance-seed/kaboo-cli、@lark-project/meegle、@openai/codex、obsidian-cli、weread-agent-cli 等。无需清理。

### 3. skills 目录整洁度

- 173 个 skill（active 113 / stale 60），无重复 SKILL.md 文件
- ⚠️ **ghost 条目**：3 个 skill 改名后（article-analysis→文章分析、client-ai-scenario-extract→客户AI场景需求整理、biweekly-skill-sync→双周Skill同步），`.usage.json` 残留旧目录名条目（last_used=None），虚增「未使用」计数。属轻量 housekeeping，不影响运行。

### 4. cronjob 列表

5 个 job 全部 active，上次运行均 **ok**，无失败：

| Job | 计划 | 上次运行 |
|-----|------|---------|
| 每日认知对话 | 5 9 * * * | ✅ 08-14 |
| 每周综合复盘 | 0 18 * * 5 | 🔄 运行中（本次） |
| 每日工作摘要 | 0 21 * * * | ✅ 08-13 |
| 每周深读推荐 | 0 10 * * 0 | ✅ 08-09 |
| 双周Skill同步 | 0 10 * * 5 | ✅ 08-14 |

⚠️ 「每周深读推荐」的 Skills 字段仍引用已删除的 `feishu-api` skill（MCP 移除后不存在），运行未受影响，建议下次顺手清理。

### 5. config.yaml 关键配置

- `model.routing` ✅ 已开启（≤500 字走 deepseek-v4-flash，复杂走 v4-pro）
- `approvals.cron_mode: deny`（正常，cron 无用户在场）
- ⚠️ **memory 已 99% 满（2190/2200 字符）** — 见紧急待办

---

## 三、技能库审计

### 统计

| 指标 | 数值 |
|------|------|
| 总量 | 173（active 113 / stale 60） |
| 近 7 天使用 | 31 |
| 近 7 天更新 | 4（obsidian、knowledge-capture、meeting-audit、wayfinder-requirements） |
| 30 天未使用（active 状态） | 40 |

### 30 天未使用候选清理（active 且 last_used=None 或 >30d）

含 article-analysis / client-ai-scenario-extract / biweekly-skill-sync 三个 ghost 条目（改名残留，可先清）。其余真实未使用如：lark-apps、github-auth、writing-plans、to-spec、loop-engineering-pipeline、three-stage-pipeline、mcporter、native-mcp、whisper、find-nearby、docx、xlsx、pdf 等。建议下轮 curator 统一评估，不必手动逐条删。

### 缺失覆盖（从本周返工反推）

| 缺口 | 状态 |
|------|------|
| Obsidian 多库/同步管理 | ✅ 已用记忆规则 + obsidian skill 更新弥补 |
| 会议旁听「用户离会」场景 | ✅ 已固化进 meeting-audit skill |

---

## 四、紧急待办处理

| 项 | 处理 |
|----|------|
| token 过期 | ✅ 已触发刷新，新有效期至 08-14 20:03（refresh 至 08-21） |
| MCP/npm 残留 | ✅ 无，无需操作 |
| CLI 过旧 | ✅ 1.0.84 已是最新 |
| memory 99% 满 | ⚠️ 需关注：建议近期跑一次 memory-compress（OptMem 四层金字塔压缩），否则下次写入会顶满。当前 2190/2200 字符 |

---

## 本周一句话总结

旁听/会议脚本的「结束判断」系统性缺陷彻底修复，Obsidian 双库分叉被揪出并建立同步规则；配置侧 token、npm、CLI 全部健康，唯一需要留神的是 memory 已顶到 99%。
