# 每周综合复盘 · 2026-W34

> 复盘周期：2026-08-14（周五 18:00）～ 2026-08-21（周五 18:00）
> 生成时间：2026-08-21 18:05 · 自动 cron（每周五 18:00）

---

## 一、协作质量审计

### 本周返工统计

本周非 cron 会话 **32 个**，识别 **7 次**用户纠正/重新指示：

| # | 日期 | 事件 | 根因分类 | 处理 |
|---|------|------|---------|------|
| 1 | 08-16 | 用户指出「pro 太费钱了，没什么任务需要用 pro 吧」 | 模型选错/配置 | ✅ 已修复（routing 关闭 + 记忆固化 flash 默认，2026-08 用户定） |
| 2 | 08-18 | Kimi Code 交付后照片仍是占位、图片框未调整，用户指出「照片还是待补充」 | 工具失败（外部 Agent 交付） | ✅ 部分修复（验收改为拉 repo + 线上实页双验证；feishu-agent-media-handoff 08-18 更新） |
| 3 | 08-18 | 汇报 Kimi Code 状态时用户质疑「这个回复是你自己好吧」 | 外部 Agent 状态误报 | ⚠️ 未结构化修复（应明确标注信息来源/证据，勿把轮询结果当对方回复） |
| 4 | 08-18 | 委派任务书写「免费」，用户纠正「不要写免费，也不写价格」 | 误解需求（对外表述未确认） | ✅ 本次已修复（agent-group-collab/kimi-code-delegation 补第 6 条「对外表述先确认再下发」） |
| 5 | 08-19 | 「怎么重复发了两条今日工作摘要？」（两次追问） | 配置问题（cron 重复投递） | ✅ 已修复（daily-session-summary 加「防重复铁律」：Evaluator 先评分后发送 + 发卡后最终响应必须 [SILENT]；08-20 运行已验证单条投递） |
| 6 | 08-19 | 会议旁听用户退会，纠正「我只是退会而已，你继续留在会上」 | 技能缺失（离会边界） | ✅ 已修复（feishu-meeting-listen 08-21 patch + 记忆固化「离会→bot join 续听」规则） |
| 7 | 08-21 | 会议纪要 C360 查错主体，用户纠正「C360 应该是逸文科技有限公司」 | 误解需求（检索对象错） | ⚠️ 单次未复发，待观察；建议 meeting-minutes 流程加「客户主体以用户指定为准，查前复述确认」 |

另有：**输出截断 5+ 次**（[System: truncated]：08-14 ×1、08-15 ×4），长输出反复撞 `max_tokens=4096` 上限，截断后续写浪费额外轮次。

**根因分布**：误解需求 ×3，工具失败 ×2，模型选错 ×1，配置问题 ×1。

### 模式问题（系统性缺陷，≥2 次）

| 缺陷 | 出现次数 | 状态 |
|------|---------|------|
| 🔴 外部编码 Agent（Kimi Code）协作链摩擦（交付不实/状态误报/媒体交接卡点/任务书表述） | 4 次（08-18 全天） | 🟡 修复中——media-handoff skill 已更新、验收流程已拉 repo 双验证、本次又补「表述确认」规则；遗留：状态汇报真实性 |
| 🟠 长输出截断 | 5+ 次 | ⚠️ 建议 `max_tokens` 4096→8192 或长卡片分块输出（见紧急待办） |
| 🟡 目标/主体确认缺失（表述口径、C360 主体） | 2 次 | 🟡 已补 1 处（Kimi 委派），C360 主体校验待固化 |

### 本周结构性修复清单

1. **daily-session-summary**（08-19 patch）：Cron 模式防重复铁律——Evaluator 先评分（三维度 ≥7）后发送；发卡后最终响应必须 `[SILENT]`；二选一禁止双投
2. **feishu-meeting-listen**（08-21 patch）：用户离会后续听边界
3. **agent-group-collab/kimi-code-delegation**（本次复盘 patch）：任务书「对外表述先确认再下发」（免费/价格/承诺类文案）
4. **记忆固化**：flash 默认省钱优先（08-16）、离会 bot 续听（08-19）

---

## 二、配置与系统审计

### 1. 核心 CLI 状态

| 项 | 状态 |
|----|------|
| lark-cli user token | ⚠️ 初始 needs_refresh（15:32 已过期）→ ✅ 触发 API 自动刷新，valid 至 08-21 20:05，refresh 至 08-28 18:05 |
| lark-cli bot | ✅ ready |
| @larksuite/cli 版本 | 1.0.84（无更旧残留） |

### 2. npm 全局包

✅ **无 MCP 残留、无垃圾包**。11 个全局包全部合法（@larksuite/cli、@customer360/lark-c360、@bytedance-seed/kaboo-cli、@lark-project/meegle、@openai/codex、obsidian-cli、weread-agent-cli 等）。无需清理。

### 3. skills 目录整洁度

- 237 个 SKILL.md（W33 为 173，近两周新增多为 Hermes 库技能：creative/mlops/github/autonomous-ai-agents 等批量导入）
- stackpilot/skills 是 → claude-config/skills 的 symlink，**非重复** ✅
- 无重复 SKILL.md 文件；W33 记录的 `.usage.json` ghost 条目（article-analysis 等改名残留）仍存在，属轻量 housekeeping

### 4. cronjob 列表

5 个 job 全部 enabled，上次运行均 **ok**，无失败：

| Job | 计划 | 上次运行 |
|-----|------|---------|
| 每日认知对话 | 5 9 * * * | ✅ 08-21（第 73 次） |
| 每周综合复盘 | 0 18 * * 5 | 🔄 运行中（本次） |
| 每日工作摘要 | 0 21 * * * | ✅ 08-20（防重复修复后单条投递验证通过） |
| 每周深读推荐 | 0 10 * * 0 | ✅ 08-16 |
| 双周Skill同步 | 0 10 * * 5 | ✅ 08-21 |

⚠️ W33 记录的「每周深读推荐」Skills 字段残留已删除的 `feishu-api` 引用仍未清理，运行不受影响，建议下次顺手从 jobs.json 移除。

### 5. config.yaml 关键配置

- `model.routing.enabled: false` ✅ 符合用户 2026-08 决定（省钱优先，默认 deepseek-v4-flash）
- `approvals.cron_mode: deny` ✅ 正常
- `cron.wrap_response: true` ✅
- `memory` 80%（1768/2200）✅ 健康（W33 的 99% 已缓解）
- 无旧 MCP 回调/残留配置 ✅

---

## 三、技能库审计

### 统计

| 指标 | 数值 |
|------|------|
| 总量 | 237 个 SKILL.md |
| 本周加载 | 32 |
| 本周更新（patch） | 3 + 1（daily-session-summary、feishu-meeting-listen、steelman-dual、agent-group-collab） |
| 近 14 天更新 | 58（多为库技能批量更新） |
| >90 天未动 | ~80（绝大多数为 Hermes 内置库：mlops/creative/stackpilot/pipeline 等，惰性无害） |

### 30 天未使用候选

用户自有 7 skill（agent-group-collab、meeting-followup-material、feishu-meeting-listen、meeting-minutes、meeting-audit、chinese-search-workaround、article-analysis）全部在用或近期维护 ✅。>90 天未动的 ~80 个多为内置库技能，建议交给 curator（stale_after_days=30 已配置）统一归档，不必手动逐条删。

### 缺失覆盖（从本周返工反推）

| 缺口 | 状态 |
|------|------|
| 委派任务书「对外表述确认」 | ✅ 已补（kimi-code-delegation.md 第 6 条） |
| 外部 Agent 状态汇报真实性标注 | ⚠️ 建议 agent-group-collab 加「区分轮询事实 vs Agent 自述」约定 |
| C360 客户主体校验（查前复述） | ⚠️ 建议 meeting-minutes 流程固化 |
| 长输出分块/截断规避 | ⚠️ 建议 max_tokens 调优（见下） |

---

## 四、紧急待办处理

| 项 | 处理 |
|----|------|
| token 过期 | ✅ 已触发自动刷新（API 调用触发），user 身份 valid 至 08-21 20:05，refresh token 至 08-28 18:05 |
| MCP/npm 残留 | ✅ 无，无需操作 |
| CLI 过旧 | ✅ 1.0.84 已是最新 |
| 输出截断（5+ 次/周） | ⚠️ 建议项：`config.yaml` 的 `model.max_tokens: 4096` → 8192（flash 单价低，收益是少 5+ 次截断续写轮次；如担心成本可只对深度任务切 pro 时提）。预计 1 分钟，可下次对话时说一声我改 |

---

## 本周一句话总结

返工 7 次创近几周新高，但**全部指向两类可修复问题**：Kimi Code 协作链（4 次，已补两条规则）和 cron 重复投递（已用 [SILENT] 铁律根治并验证）；token 已自动刷新、npm 干净、5 个 cron 全绿；最大遗留是输出截断，建议把 max_tokens 翻倍一劳永逸。
